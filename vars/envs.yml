---
environment_vars:
  TEST_NAME: some_value
  DATABASE_HOST: localhost
  DATABASE_NAME: "{{ postgres_db_name }}"
  DATABASE_USERNAME: "{{ postgres_db_user }}"
  DATABASE_PASSWORD: "{{ postgres_db_password }}"

  RAILS_MASTER_KEY: your_rails_app_master_key
  SECRET_KEY_BASE: your_rails_app_secret_key_base